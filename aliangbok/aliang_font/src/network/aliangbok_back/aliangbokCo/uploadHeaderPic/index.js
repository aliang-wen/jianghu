import { API } from '../../../../api';

export const uploadHeaderPic = params => {
  return hyNetWork.post(API.uploadHeaderPic, params).then(res => res.data);
};
export const sssssssssssssss = params => {
  return hyNetWork.post(API.getSecuritupdateHeaderPicyCode, params).then(res => res.data);
};
